# Governance Rules — SUPERSEDED AS AGENT INSTRUCTIONS

The five principles previously stored here are preserved by the current repository controls, but this file is no longer a separate instruction source for agents.

Start at [`../AGENT_KERNEL.md`](../AGENT_KERNEL.md).

The durable principles remain:

- human authority is external to the model;
- safety boundaries fail closed;
- promotion/deployment authority is explicit;
- memory/source claims must resolve to authoritative sources rather than invented state;
- capability enablement follows the live implementation and its enforced configuration, not stale prose.

This file remains only as a compatibility pointer for old links.
