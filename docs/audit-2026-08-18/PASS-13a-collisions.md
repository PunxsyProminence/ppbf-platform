# Pass 13a — Cross-cutting synthesis: collisions between passes

**Status: IN PROGRESS.** Written incrementally, section by section, per instruction — an
incomplete file with real content is the goal if this session runs out of budget before
finishing.

## Method

Read `README.md` in full first (it already aggregates a great deal of cross-pass
correction from the lead session — that is itself an input here, not a substitute for
reading the sixteen pass files, since the README's aggregation is one person's synthesis
across time, not a structured hunt for cross-pass collisions). Then read every
`PASS-*.md` and `VERIFY-*.md` file in full. For each of the six directed collision
hunts, cross-reference the named passes, re-quote their load-bearing sentences, and
verify against source at `path:line` before writing anything down. Additionally scan
every pass's "checked and found sound" / "no finding here" language against every other
pass's finding table for direct contradictions (collision 6).

Working notes are appended below as each pass is read; the running list of candidate
collisions is refined as more passes are read, and candidates that do not survive
scrutiny are recorded as ruled out rather than dropped silently.

---

