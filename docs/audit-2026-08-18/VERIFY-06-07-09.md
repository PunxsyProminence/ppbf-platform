# Verification — Passes 6, 7, 9

Refutation pass over `PASS-06-data-layer.md`, `PASS-07-frontend.md` and
`PASS-09-formulas.md`. Every finding was treated as wrong until the file proved
otherwise. Each subsection carries an independent verbatim quote with
`path:line`, the strongest refutation that could be constructed against the
finding, and whether that refutation succeeded.

Baseline: branch `docs/full-spectrum-audit-2026-08-18`, reconciled against
`origin/main` at `0485cf81` (twelve PRs merged since the three passes ran,
including #452, #459, #460, #461, #462, #463, #412, #422, #453, #455, #457,
#458). Findings were re-checked against current file state, not the state the
passes saw.

## Summary

<!-- table appended after all verdicts settled -->

## Pass 6

### P6-01 [HIGH] Soft delete is written, indexed for, and filtered by nothing

**Verdict: CONFIRMED WITH CORRECTION** — severity stays **HIGH**.

**My own evidence, independent of the finding's.**

The soft-delete write, character-exact:

```
    const deleted = await client.query<{ deleted_at: string }>(
      `update pilot.accounts
       set deleted_at = now(), updated_at = now()
       where account_id = $1
       returning deleted_at::text as deleted_at`,
```
— `apps/web/src/server/pilot/dataDeletion.ts:62-66`

Nothing else is set. `active_flag` is untouched. The login path selects
`active_flag` and never `deleted_at`:

> `       a.active_flag,`
> `       a.has_master_shadow_access,`
> `       o.status as organization_status`
> `     from pilot.accounts a`
— `apps/web/src/server/pilot/auth.ts:110-113`

and rejects on that column alone:

> `  if (!data?.active_flag) {`
— `apps/web/src/server/pilot/auth.ts:127`

The same shape repeats at `auth.ts:208` (Microsoft federated login) and
`auth.ts:297` (session-token resolution). `deleted_at` appears nowhere in
`auth.ts` — `grep -c deleted_at apps/web/src/server/pilot/auth.ts` returns 0.
The deletion route performs no session revocation either: it calls
`deleteGuardianAccount` / `deleteAthleteRecord` and returns
(`apps/web/app/api/pilot/admin/data-deletion/route.ts:50`, `:59`), so an
existing session survives *and* a fresh login succeeds.

**File-level enumeration I ran myself.** 55 non-test files under `apps/web`
reference `pilot.athletes`. Exactly **two** of them mention `deleted_at`:
`src/server/pilot/dataDeletion.ts` and `scripts/pilot-cleanup-deleted-data.mjs`
— i.e. the writer, the purge and the status report. **53 files read
`pilot.athletes` with no `deleted_at` predicate**, including every reader the
finding names. Representative, and character-exact:

> `      'select * from pilot.athletes where organization_id = $1 order by created_at desc',`
> — `apps/web/src/server/pilot/entities.ts:259`

The partial index the migration builds for a filter nobody applies is also
character-exact as quoted:

> `-- --- Partial indexes: the active-record path, which is every read ---`
> `create index if not exists idx_athletes_active_org`
> `  on pilot.athletes(organization_id, athlete_id)`
> `  where deleted_at is null;`
> — `infra/azure/pilot_slice_postgres_data_retention_deletion_migration.sql:25-28`

**Strongest refutation I could construct, and why each failed.**

1. *A filtered view, rule or RLS policy hides the rows.* Failed. `deleted_at`
   occurs in exactly three SQL files (`pilot_slice_postgres.sql:896,906` —
   which is a different table's own column, `shadow_runtime_migration.sql:12,21`,
   and the retention migration). No `create view` or `create policy` over
   `pilot.athletes` or `pilot.accounts` exists.
2. *The soft delete also clears `active_flag`, so the login check covers it.*
   Failed for the route path — the UPDATE above sets two columns. It **does**
   hold for a *different* path: `apps/web/scripts/pilot-cleanup-accounts.mjs:247`
   sets `deleted_at = now()` *and* clears `active_flag`, and its header states
   `SOFT DELETE ONLY       Sets deleted_at, clears active_flag, revokes`
   (`:29`). So the operator CLI cannot produce a logging-in ghost; the
   right-to-be-forgotten API route can. This is a real narrowing of the
   finding's blast radius that the finding does not mention.
3. *Some reader filters indirectly (membership `active_flag`, org status).*
   Failed for athletes — athletes have no `organization_memberships` row keyed to
   the athlete record, and `getAthletesByOrganization` above filters on
   `organization_id` only.
4. *Fixed by a merge since the pass ran.* Failed. None of the twelve merged PRs
   touches `dataDeletion.ts`, `auth.ts`'s account predicates, or adds a
   `deleted_at` filter; the current file state is as quoted above.

**Corrections the finding needs.**

- **The route verb is wrong.** The finding writes "*`POST
  /api/pilot/admin/data-deletion` calls both functions*". The route exports only
  `DELETE`: `export async function DELETE(request: NextRequest) {`
  (`apps/web/app/api/pilot/admin/data-deletion/route.ts:21`). There is no `POST`
  handler in the file. The cited line numbers 50 and 59 are correct.
- **The mechanical enumeration is not reproducible as stated.** The finding
  claims "*76 non-test statements reference `pilot.athletes`; 4 mention
  `deleted_at`, and all four are inside `dataDeletion.ts` itself*" and "*61
  non-test statements reference `pilot.accounts`; 3 mention `deleted_at`, all
  three again in `dataDeletion.ts`*". A raw grep of non-test `apps/web` gives
  112 line-references for `pilot.athletes` and 114 for `pilot.accounts`, so
  "statements" is some unstated de-duplication I could not reproduce. More
  concretely, *"all four are inside `dataDeletion.ts`"* is false as written:
  `apps/web/scripts/pilot-cleanup-deleted-data.mjs:90` and `:132` both query
  `pilot.athletes` on `deleted_at`, and that file is under `apps/web` and is not
  a test. Likewise `apps/web/scripts/pilot-approve-library-baseline.mjs:151`
  applies `and deleted_at is null` to `pilot.accounts` — and it is the one place
  in the repository that gets the semantics right, with the reasoning written
  down: *"An inactive or soft-deleted owner is not eligible -- an attestation by
  an account that cannot sign in is not an attestation."* (`:151` region,
  `:142-144`). The count is wrong and the "only in dataDeletion.ts" framing
  erases the single counter-example that proves the codebase knows the filter
  is needed.
- **The quoted UPDATE is re-indented.** The finding's fenced quote renders
  `set deleted_at = now(), updated_at = now()` with nine leading spaces; the
  file has seven (`dataDeletion.ts:64`). Cosmetic, but it is inside a code
  fence presented as verbatim, and the cited range `63-65` is one line off from
  the statement's true span `62-66`.
- **"Everywhere" survives, "a deleted guardian keeps logging in" survives with
  a scope note** — true via the API route, not via the operator CLI.

**Why severity holds at HIGH.** 53 of 55 reader files admit soft-deleted
minors; the login path has no `deleted_at` predicate at any of its three
entry points; no session is revoked. The direction of failure is
over-retention and continued access by a guardian the gym believes it removed.

---

### P6-02 [HIGH] The retention hard-delete cannot succeed: foreign keys with no `on delete` action block the purge

**Verdict: CONFIRMED WITH CORRECTION** — severity stays **HIGH**. The headline
count "two foreign keys" is materially wrong, in the direction that makes the
problem *larger*.

**My own evidence.** I re-derived the FK census with a script over
`infra/azure/*.sql`, checking each `references` line plus the two lines after it
for an on-delete action (multi-line constraint bodies put the action on its own
line):

- `pilot.athletes`: **74** inbound FK references. **73** carry `on delete
  cascade`; **1** carries no action at all. The one is exactly the constraint the
  finding names, and it is character-exact at the cited lines:

> `  constraint pilot_one_percent_nominations_athlete_fk`
> `    foreign key (organization_id, athlete_id) references pilot.athletes(organization_id, athlete_id),`
> — `infra/azure/pilot_slice_postgres_one_percent_club_migration.sql:61-62`

- `pilot.accounts`: **127** inbound FK references — **34** `on delete cascade`,
  **27** `on delete set null`, and **66 with no on-delete action at all.**

The `pilot.parents.account_id` reference the finding names is one of those 66,
and is character-exact:

> `  account_id text null references pilot.accounts(account_id),`
> — `infra/azure/pilot_slice_postgres.sql:267`

and it sits inside `create table if not exists pilot.parents (` at
`pilot_slice_postgres.sql:264`, which is what makes it the always-populated one:
every guardian invite writes the row —

> `        `insert into pilot.parents (organization_id, parent_id, account_id, full_name, email)`
> — `apps/web/src/server/pilot/staffProvisioning.ts:470`

Both deletes plus the audit insert are one transaction
(`apps/web/scripts/pilot-cleanup-deleted-data.mjs:85` `await
client.query('begin');` … `:154` `await client.query('commit');`), with the
athlete delete first at `:131-132` and the account delete at `:136-139`, and the
failure path emits an identifier only:

> `      event: 'retention.cleanup.failed',`
> `      reason: error instanceof Error ? error.name : 'UnknownError',`
> — `apps/web/scripts/pilot-cleanup-deleted-data.mjs:168-169`

**Strongest refutation I could construct, and why each failed.**

1. *Something pre-deletes the referencing rows so the purge succeeds anyway.*
   Failed, and I checked both implementations. `purgeExpiredDeletedData`
   (`dataDeletion.ts:199-249`) issues exactly two `delete` statements plus the
   audit insert. The CLI (`pilot-cleanup-deleted-data.mjs:80-175`) issues one
   count, two deletes, one audit insert, `commit`. Neither touches
   `pilot.one_percent_nominations` or `pilot.parents`. There is no
   `on delete cascade` to lean on and no manual cleanup.
2. *The constraints are `deferrable`, so the check comes at commit and the
   ordering saves it.* Failed harder than the finding claims: `grep -rn
   deferrable infra/azure/*.sql` returns **nothing at all** — no constraint
   anywhere in the schema is deferrable.
3. *A later migration adds the cascade.* Failed. `one-percent-club` is genuinely
   the final element of the `all` loop —
   `for m in multiorg … attendance-precedence one-percent-club; do`
   (`.github/workflows/apply-migrations.yml:279`) — and `grep -rn "alter table
   pilot.parents" infra/azure/*.sql` returns nothing.
4. *The blocking table is dead, so no rows ever exist.* Failed. Nominations are
   written by live code — `insert into pilot.one_percent_nominations (`
   (`apps/web/src/server/pilot/onePercentClub.ts:275`) behind
   `apps/web/app/api/pilot/coach/one-percent-club/route.ts`.
5. *Fixed by a merge since the pass ran.* Failed. No merged PR alters either
   constraint or adds a pre-delete.

**Corrections the finding needs.**

- **"Two foreign keys with no `on delete` action" is wrong.** It is accurate for
  the athlete half (1 of 74) and wrong for the account half: 66 of 127 inbound
  FKs to `pilot.accounts` carry no action. The finding presents
  `pilot.parents.account_id` as *the* blocker rather than as the one that is
  guaranteed to be populated. Choosing the always-populated instance as the
  existence proof is sound method; stating the census as "two" is not, and it
  understates how much work fixing this is.
- **"Cannot succeed" needs one qualifier.** The purge short-circuits before any
  delete when there is nothing due: `if (total === 0) { await
  client.query('rollback'); … 'retention.cleanup.completed' }`
  (`pilot-cleanup-deleted-data.mjs:126-129`). So an empty run reports success
  honestly. The precise claim is: *any run with a due parent account aborts*
  (guaranteed, via `pilot.parents`), and *any run with a due athlete who was ever
  nominated aborts* (conditional). Both abort the whole transaction for every
  family in it. Stated that way the finding is airtight; stated as an
  unconditional "cannot succeed" it is refutable on an empty database.
- **The design-comment quote is reflowed.** The finding renders the file's
  reasoning as *"Nothing is deleted to resolve a nomination -- a withdrawal
  stays on the record"* citing `:65-66`. The file has it across two lines with a
  trailing clause the quote drops:
  `  -- Nothing is deleted to resolve a nomination -- a withdrawal stays on the`
  (`:65`) / `  -- record, and it has to say why.` (`:66`). Joined and truncated
  inside quote marks.

The workflow quote is exact: `#   The dry run is not busywork: it is the
monitor. A number that climbs` (`.github/workflows/retention-cleanup.yml:35`).

**Why severity holds at HIGH.** Direction of failure is safe (nothing
over-deleted), but a published retention commitment is unexecutable and the
only signal is an error *name* in a CI log. The correction raises the remediation
cost rather than lowering the risk.

---

### P6-03 [MEDIUM] `pilot.video_sessions` has no FK to organization, athlete or uploader

**Verdict: CONFIRMED** — severity stays **MEDIUM**.

My own quote, character-exact at the cited range:

> `--   * organization_id has no foreign key to pilot.organizations, unlike every`
> `--     other multi-org table.`
> `--   * uploaded_by_account_id has no foreign key to pilot.accounts.`
> `--   * athlete_id has no composite foreign key to pilot.athletes.`
> — `infra/azure/pilot_slice_postgres_video_sessions_migration.sql:50-53`

The admin branch of the list route is unfiltered by anything but org, as claimed:

> `         from pilot.video_sessions`
> `         where organization_id = $1 ${athleteFilter}`
> — `apps/web/app/api/pilot/video/list/route.ts:120-121`

**Strongest refutation.** I tried the same door the finding tried and one more.
(a) *A later `alter table … add constraint … foreign key` supplies the FKs.*
Failed, and the finding's count is exactly right: the whole of `infra/azure/*.sql`
contains **one** such statement, and it is templated over `organization_id`:
`'alter table %s add constraint %I foreign key (organization_id) references
pilot.organizations(organization_id)'`
(`infra/azure/pilot_slice_postgres_multiorg_migration.sql:185`). (b) *The blob is
removed by a separate sweep so only a metadata row is orphaned.* Not established
either way here; Pass 3's F-17 already owns the blob half, and this finding
correctly says so.

The finding's own bounding — latent, because P6-02 means the athlete hard-delete
cannot run — is honest and I did not find a way to make it live.

---

### P6-04 [MEDIUM] The org-wide consent console is 1 + 2N queries, unbounded

**Verdict: CONFIRMED WITH CORRECTION** — severity stays **MEDIUM**. The arithmetic
in the headline is an upper bound, not the count.

My own quotes:

> `  return Promise.all(`
> — `apps/web/src/server/pilot/guardianConsent.ts:317`

> `    const rows = await listOrganizationConsentStatus(principal.organizationId);`
> — `apps/web/app/api/pilot/admin/athlete-consent/route.ts:22`

Both character-exact. The two per-athlete queries are at
`guardianConsent.ts:107` (`select parent_id from pilot.guardian_links where
organization_id = $1 and athlete_id = $2`) and `:73` (the `distinct on
(parent_id)` over `pilot.waivers`).

**Strongest refutation, and it lands a correction.** `checkGuardianMediaConsent`
short-circuits before its second query when the athlete has no guardian link:

> `  if (guardianIds.length === 0) {`
> `    return { ok: false, guardianIds: [], missingParentIds: [], perGuardian: [] };`
> — `apps/web/src/server/pilot/guardianConsent.ts:112-113`

So the true cost is `1 + N + G` where `G` is the number of athletes with at least
one guardian link — not `1 + 2N`. For a fully-linked roster the two are equal, so
the finding's "200-athlete gym issues 401 queries" is the worst case rather than
the count; on a roster with unlinked athletes it is lower. The refutation does
not defeat the finding, it bounds it.

The finding's own distinction — that the unbounded read is a *recorded* decision
(`guardianConsent.ts:300-305`, which I read and which says what the finding says
it says) and the 2N round trips are not — is correct and is the right place to
put the weight. The single-query shape it points at genuinely already exists in
the codebase on the same two tables: `left join lateral ( select distinct on
(waiver_type) waiver_type, status from pilot.waivers …`
(`apps/web/src/server/pilot/waiverCompliance.ts:48-55`).

---

### P6-05 [MEDIUM] Every emergency contact defaults to "primary"; nothing enforces one; the roster export picks the oldest

**Verdict: CONFIRMED WITH CORRECTION** — severity stays **MEDIUM**.

My own quotes, all character-exact at the cited lines:

> `  is_primary boolean not null default true,`
> — `infra/azure/pilot_slice_postgres.sql:384`

> `      params.isPrimary ?? true,`
> — `apps/web/src/server/pilot/intake.ts:438`

The table's only constraints really are the PK and the athlete FK
(`pilot_slice_postgres.sql:388-389`) and its only index really is
`idx_pilot_emergency_contacts_org_athlete on
pilot.emergency_contacts(organization_id, athlete_id, created_at desc)` (`:467`).

**Strongest refutation, and it produces one correction plus one point that
*strengthens* the finding.**

- *There is a second write path that demotes siblings.* Failed for
  `emergency_contacts`, but the finding's phrasing is wrong: it says
  "*`is_primary` appears in exactly one write path (`intake.ts:438`) and is
  otherwise read-only*". Repo-wide that is false — `pilot.board_seats.is_primary`
  has a full demotion path: `       set is_primary = false, updated_at = now()` /
  `       where organization_id = $1 and seat = $2 and account_id <> $3 and
  is_primary`, (`apps/web/src/server/pilot/boardSeats.ts:358-359`). Scoped to
  `emergency_contacts` the claim holds. **And this makes the finding stronger,
  not weaker:** the codebase already implements exactly the sibling-demotion the
  emergency-contact table lacks, one module away, so this is an inconsistency
  rather than an unsolved problem.
- Two callers, not one, supply `isPrimary`, and both default to true:
  `        isPrimary: Boolean(body.payload.is_primary ?? true),`
  (`apps/web/app/api/pilot/intake/domain-upsert/route.ts:74`) and
  `        isPrimary: promotion.emergency_contact.is_primary,`
  (`apps/web/app/api/pilot/intake/review-action/route.ts:381`).
- **Quote formatting flag.** The roster-export quote is re-indented: the finding
  renders `    order by c.is_primary desc, c.created_at asc` / `    limit 1` with
  four leading spaces; the file has six (`route.ts:81-82`). Content exact,
  indentation not.

---

### P6-06 [MEDIUM] Waiver consent vocabulary enforced only in TypeScript, reaching the column from a request body

**Verdict: CONFIRMED** — severity stays **MEDIUM**.

My own quotes, all character-exact:

> `  waiver_type text not null,`
> — `infra/azure/pilot_slice_postgres.sql:413`

> `  status text not null,`
> — `infra/azure/pilot_slice_postgres.sql:418`

> `  waiverType: string;`
> — `apps/web/src/server/pilot/intake.ts:483`

> `  status: string;`
> — `apps/web/src/server/pilot/intake.ts:488`

**Strongest refutation.** (a) *A CHECK is added by a later migration.* Failed —
the guardian-media-consent migration extends `pilot.waivers` with a parent FK
(`:66-70`) and no vocabulary constraint. (b) *An out-of-vocabulary value fails
open somewhere.* Failed, and the finding already ran this and reported the
result honestly:
`  const missingParentIds = perGuardian.filter((g) => g.status !== 'signed').map((g) => g.parentId);`
(`guardianConsent.ts:126`) treats an unrecognised status as *not consented*, and
the transactional variant is the same shape
(`  const missingParentIds = guardianIds.filter((id) => current.get(id)?.status !== 'signed');`,
`guardianConsent.ts:180`). Fail-closed in both. The finding self-downgraded to
MEDIUM on exactly this basis, which is the correct call.

---

### P6-07 [LOW] `pilot_waivers_parent_fk` — `on delete set null` on a composite whose first column is `not null`

**Verdict: CONFIRMED** — stays **LOW**.

> `      foreign key (organization_id, parent_id) references pilot.parents(organization_id, parent_id)`
> `      on delete set null;`
> — `infra/azure/pilot_slice_postgres_guardian_media_consent_migration.sql:69-70`

Character-exact. `  organization_id text not null references pilot.organizations(organization_id),`
at `infra/azure/pilot_slice_postgres.sql:410` confirms the not-null half.

**Strongest refutation.** *A live code path already deletes `pilot.parents`, so
this is live rather than latent.* Failed and the finding's census is exactly
right: `delete from pilot.parents` occurs three times, at
`guardianClaimOnInvite.pg.test.ts:238`, `duplicateGuardianCheck.pg.test.ts:211`
and `apps/web/scripts/rehearse-seed-guardians.mjs:210`. No application path. LOW
is correct.

---

### P6-08 [LOW] `pilot.organization_memberships.account_id` has no FK to `pilot.accounts`

**Verdict: CONFIRMED** — stays **LOW**.

> `create table if not exists pilot.organization_memberships (`
> `  account_id text not null,`
> — `infra/azure/pilot_slice_postgres.sql:15-16`

Character-exact, and the contrast the finding draws is on the very next line:
`  organization_id text not null references pilot.organizations(organization_id) on delete cascade,`
(`:17`).

**Strongest refutation.** *An orphan membership grants a role.* Failed, and the
finding says so itself: `resolvePrincipal` inner-joins `pilot.accounts` and
requires `and om.active_flag = true` (`apps/web/src/server/pilot/auth.ts:290`),
so a membership row with no account resolves nothing. Correctly bounded to LOW.

---

### P6-09 [LOW] No SQLSTATE mapping; one module relies on the 500 path by design

**Verdict: CONFIRMED** — stays **LOW**.

> `    return NextResponse.json({ error: 'Internal server error' }, { status: 500 });`
> — `apps/web/src/server/pilot/http.ts:171`

> ` * does not decide anything itself. status='current' requires`
> ` * verified_by_account_id, verified_at, and issued_on`
> ` * (pilot_person_clearances_current); callers passing status='current'`
> ` * without a verifier hit that constraint, same as any other write path.`
> — `apps/web/src/server/pilot/clearanceRegister.ts:133-136`

Both character-exact.

**Strongest refutation.** *`recordPersonClearance` has callers, so the
acknowledged reliance is live.* Failed. Non-test references are its own
definition (`clearanceRegister.ts:143`) and one comment (`:18`). Zero callers,
as the finding states. LOW is correct.

---

### P6-10 [LOW] `pilot.mixed_age_session_records.youth_athlete_id` has no FK while its sibling does

**Verdict: CONFIRMED** — stays **LOW**.

> `  youth_athlete_id    text not null,`
> — `infra/azure/pilot_slice_postgres_multidiscipline_migration.sql:155`

Character-exact, and the sibling contrast is exact too:
`  constraint pilot_grappling_exposure_athlete_fk foreign key (organization_id, athlete_id)` /
`    references pilot.athletes(organization_id, athlete_id) on delete cascade,`
(`:102-103`). The adjacent column *does* carry an FK —
`  adult_account_id    text not null references pilot.accounts(account_id),`
(`:156`) — so within one table the adult is referentially bound and the child is
not, which is a sharper framing than the finding uses.

**Strongest refutation.** *A write path exists.* Failed: non-test references to
`recordMixedAgeSession` are its definition (`multidiscipline.ts:308`) and the
route comment saying it has no caller
(`apps/web/app/api/pilot/multidiscipline/route.ts:15`). LOW correct.

---

### P6-11 [LOW] `pilot.sparring_exposure.sparring_type` has no CHECK; the counter would return `NaN`

**Verdict: CONFIRMED** — stays **LOW**.

> `  sparring_type       text not null,   -- hard | play | technical | game | conditioned`
> — `infra/azure/pilot_slice_postgres_sparring_exposure_and_load_migration.sql:56`

> `    segmentsByType[segment.sparring_type] += 1;`
> — `apps/web/src/server/pilot/sparringExposure.ts:206`

Both character-exact.

**Strongest refutation.** *A route reaches `recordSparringExposure`.* Failed: the
only non-test reference in the repository is its own definition
(`sparringExposure.ts:111`). The `NaN` is real and unreachable. LOW correct.

---

### P6-12 [LOW] `pilot.activity_log` queried by `athlete_id` with no index on it, unbounded by default

**Verdict: CONFIRMED** — stays **LOW**.

> `       and ($3::text is null or athlete_id = $3)`
> — `apps/web/src/server/pilot/activityLog.ts:175`

> `     ${filter.limit ? 'limit $7' : ''}`,`
> — `apps/web/src/server/pilot/activityLog.ts:180`

Both character-exact.

**Strongest refutation.** *An index leading on `(organization_id, athlete_id)`
exists after all.* Not refuted — the table's indexes lead on
`person_account_id` / `activity_domain`, as stated. The unbounded default is a
recorded decision the finding correctly declines to attack. LOW correct.

---

### P6-13 [LOW] `pilot.transfer_claims` has no reader; its display-control column defaults to athlete-facing

**Verdict: CONFIRMED WITH CORRECTION** — stays **LOW**.

The default is as claimed:

> `  athlete_facing    boolean not null default true,`
> — `infra/azure/pilot_slice_postgres_transfer_claims_migration.sql:56`

and there is genuinely no `select` against the table anywhere in `apps/web/src`
or `apps/web/app`.

**Strongest refutation, and it lands.** The finding says the table "*is written
only by `sessionScriptsTransfer.pg.test.ts`*". That is false: a dedicated
non-test seed script writes it —
`      `insert into pilot.transfer_claims (`
(`apps/web/scripts/seed-transfer-claims.mjs:166`) — with its own CSV input
(`seed_transfer_claims.csv`, `:205`). Further, the "which one it is needs the
owner" conclusion is already *recorded* as pending in the repo, which the finding
does not cite: `Consequence: \`pilot.transfer_claims\` ships empty.`
(`docs/current/DEPLOY_RUNBOOK_2026-08-08.md:150`) and `   The \`transfer_claims\`
table ships empty until this is settled.` (`:228`). So this is a known parked
item with an owner gate already written down, not an undiscovered orphan. The
substance (no reader, athlete-facing default) stands; the "written only by a
test" claim and the implication that the decision is unrecorded do not.

---

## Pass 7

### P7-01 [HIGH] `/simulator` renders invented coaching guidance graded on the Layer 11 safety ladder, ungated and undisclosed

**Verdict: CONFIRMED** — severity stays **HIGH**.

**Reachability, which is what I was asked to break.** It is reachable, and it is
nav-linked.

- **No page gate.** `grep -n "requirePageRole\|RoleSessionGate\|requirePrincipal\|auth"
  apps/web/app/simulator/page.tsx` returns nothing. The file's only three imports
  are `next/link`, `DevelopmentPipelineBanner` and `ShadowChatButton`
  (`apps/web/app/simulator/page.tsx:1-3`).
- **No global gate either.** There is **no `middleware.ts` anywhere in
  `apps/web`** (`find apps/web -maxdepth 2 -name middleware.ts` is empty), so
  nothing gates it above the page. This is a stronger refutation-attempt than the
  finding ran, and it also fails.
- **Nav-linked, not dead.** `  { href: '/simulator', label: 'Simulator', room: 'floor', roles: OPEN,`
  (`apps/web/components/buildingMap.ts:284`), and that file is consumed by the
  corridor nav and the card catalogue (`apps/web/components/Corridor.tsx`,
  `apps/web/components/CardCatalog.tsx`). `OPEN` is defined at
  `buildingMap.ts:51` and the file's header says what it means:
  `` * `roles: OPEN` marks a surface with no role gate at all today — sign-in,``
  (`buildingMap.ts:24`) — character-exact at the cited line.

**Content, verified independently.** Seven scenario literals
(`grep -c "    title: '"` returns 7). The risk grade is a literal:

> `    riskRating: 'High',`
> — `apps/web/app/simulator/page.tsx:18`

and it is rendered on the safety ladder's own badge classes:

> `function riskBadge(risk: 'Low' | 'Moderate' | 'High') {`
> `  if (risk === 'Low') return { className: 'badge badge--cleared', glyph: '✓', label: 'Low Risk' };`
> `  if (risk === 'Moderate') return { className: 'badge badge--restricted', glyph: '▲', label: 'Moderate Risk' };`
> `  return { className: 'badge badge--locked', glyph: '✕', label: 'High Risk' };`
> — `apps/web/app/simulator/page.tsx:60-63`

Character-exact, and the finding's cited range `:57-64` correctly spans the
comment plus the function.

**Disclaimer hunt — I looked in the places the finding did and one it didn't.**

1. *On the page.* All 159 lines read. The nearest thing is
   `  { label: 'Engine', value: 'Front-end Only' },`
   (`apps/web/app/simulator/page.tsx:70`) and
   `              Run front-end what-if scenarios to evaluate expected outcomes and risk before audit and promotion stages.`
   (`:84`, character-exact). Neither says the content is invented.
2. *In a parent layout.* There are exactly four: `apps/web/app/layout.tsx`,
   `app/athlete/layout.tsx`, `app/board/layout.tsx`, `app/wall/layout.tsx`. I
   grepped all four for `fabricated|sample data|not yet implemented|prototype|
   disclaimer|mock` — **zero hits in any of them.** `/simulator` sits under the
   root layout only.
3. *In `DevelopmentPipelineBanner`, which the page does render.* The finding
   never checked this one. I did: the component contains none of that
   vocabulary either. The refutation fails.
4. *Fixed since?* No. `git diff HEAD origin/main -- apps/web/app/simulator
   apps/web/components/buildingMap.ts` is **empty**. #422 declared four other
   prototypes and left this one alone.

**The comparison the finding draws is exact.** `/source-control` carries both
halves at the cited lines:

> `          <span className="stamp stamp--brass">{capabilityStatus}</span>`
> `          <p className="t-body max-w-[80ch]">`
> `            Shows how cards would move through Draft, Review, Approved, Published, and Archived states before ecosystem release. Every card, version, and count on this page is a sample, not live promotion state.`
> — `apps/web/app/source-control/page.tsx:96-98`

**Quote flag.** One item is a *rendered* form, not file text: the finding writes
"a `plaque` reading `ENGINE: FRONT-END ONLY` (`app/simulator/page.tsx:70`)". Line
70 reads `  { label: 'Engine', value: 'Front-end Only' },` — the uppercase,
colon-joined form is what the plaque component renders, not what the line says.
Presented in backticks against a line cite, it reads as a file quote. Cosmetic
but worth correcting since the whole finding turns on what the page literally
says.

Independent corroboration I found while checking: Pass 16 reached the same
conclusion from the opposite direction — *"Source Control declares itself,
Scenario Simulation does not"* (`docs/audit-2026-08-18/PASS-16-research-library.md:137`).

**Severity holds at HIGH.** Ungated (no page guard, no middleware), nav-linked,
seven invented coaching decisions, safety-ladder ink on a literal, no disclosure
anywhere in the render tree.

---

### P7-02 [HIGH] `/operations` stamps "Signed & Active" over safety guarantees found unenforced

**Verdict: CONFIRMED WITH CORRECTION** — severity stays **HIGH**. One cited line
range is wrong by fifteen lines and the sentence built on it points at unrelated
text.

**Verified independently and character-exact.** The stamp:

> `              <div className="mat-leather rounded-[var(--r-md)] p-[var(--s4)]">`
> `                <span className="stamp stamp--green">Signed &amp; Active</span>`
> `                <p className="t-body mt-[var(--s4)]">`
> `                  Certification Status: Signed and Active. Logical paths, equations, role boundaries, and sandbox behavior are aligned for SHADOW core build execution.`
> `                </p>`
> `              </div>`
> — `apps/web/app/operations/page.tsx:301-306`

The design comment above it, at the cited `:295-300`, is exact, including
`                  Law 7 gives it ink: a stamp on the page, in the approved`
(`:299`). And the ink really is the reserved one:
`  --stamp-green:#2F7A3E;  /* approved, compliant */`
(`design-system/ppbf.css:148`) — the finding's "line 148" is right.

The boundary checks are exact at their cited range:

> `const shadowBoundaryChecks = [`
> `  'Readiness upper bound test resolves to 10.0 and remains stable at clamp.',`
> — `apps/web/app/operations/page.tsx:126-127`

**Role gating — checked, and the finding is right.** `<details>` at `:234`
collapsed by default, `System Diagnostics and SHADOW Certification` at `:236`,
and the gate is:

> `const operationsRoles: ClubRole[] = [...roleRoutes.map((route) => route.role), 'platform_owner'];`
> — `apps/web/app/operations/page.tsx:203`

wrapped as `    <RoleSessionGate allowedRoles={operationsRoles}>` (`:207`).
`apps/web/components/roleRoutes.ts` declares **15** roles (athlete, coach, parent,
admin, staff, volunteer, `board`, plus eight named board seats at `:78-120`), so
the allow-list is 16 entries. Every role in the gym, as claimed. *Minor precision
note:* the finding enumerates "athlete, coach, parent, admin, staff, volunteer
and all eight board seats" — fourteen — and omits the generic `board` role, so
its own list is one short of what it correctly describes as "every role".

**The correction, and it is a real error in a HIGH finding's supporting text.**
The finding writes: "*and `apps/web/app/operations/page.tsx:167-172`
(`shadowComplianceChecks`)*", quotes three strings from it, and then argues
"*which is what line 167 says cannot happen*".

`shadowComplianceChecks` is declared at **line 152**, and the three quoted strings
are at **lines 153, 154 and 155**:

> `const shadowComplianceChecks = [`
> `  '12-role viewport segregation prevents cross-role data leakage.',`
> `  'Athlete view cannot mount finance/admin controls.',`
> `  'Board and governance view cannot parse raw individual biometric streams.',`
> — `apps/web/app/operations/page.tsx:152-155`

Lines 167-172 are inside an unrelated design comment about build-state badges;
line 167 actually reads
`   nothing else, and "this screen is not written yet" is neither. Twenty`. So the
quoted strings are character-exact but attributed to the wrong lines, and the
sentence "what line 167 says cannot happen" points at prose about colour tokens.
The claim survives at `:153`; the citation does not.

**Second correction.** The finding says "*`grep -rn readinessMath` across
`apps/web` returns exactly two hits, `src/server/pilot/readinessMath.ts` and its
own test file*". The count is two, but **both hits are inside the test file** —
`apps/web/src/server/pilot/readinessMath.test.ts:5` and `:7`. The module exists
(`apps/web/src/server/pilot/readinessMath.ts`, 991 bytes) and simply never
appears in any other file's text, which is a *stronger* statement of zero
importers than the finding makes. Substance holds; the description of where the
hits are does not.

**Strongest refutation I could construct.** (a) *The panel is marked
illustrative.* Failed — the `<details>` is a collapse, and the summary text
asserts diagnostics rather than disclaiming them. (b) *It is admin-only.* Failed,
per `:203` above. (c) *Fixed since?* Failed — `git diff HEAD origin/main --
apps/web/app/operations` is empty; #462 fixed the revenue centre, not this page.

**Severity holds at HIGH** on the frontend claim (governance ink on an
unsigned self-assertion, shown to every role). Note that the finding's *escalation
argument* rests on Pass 2's F-20 and Pass 4's F-08 being correct; those are other
passes' findings and are out of scope here. This verdict confirms the rendering
and the gating, not those two.

---

### P7-03 [HIGH] "The recorded basis for the 'leave the consoles alone' decision is measurably wrong: one of six carries the stamp, not six"

**Verdict: OVERSTATED as of current `origin/main`** — corrected severity **LOW**.
The count was **correct** at the commit the pass read; #422 has since closed four
of the five gaps.

**I checked all six consoles individually, at both commits.** The six pages and
their seven components, resolved from the page wrappers:

| # | Page | Component(s) | Stamp @ 04dd116b | Disclaimer @ 04dd116b | Stamp @ origin/main | Disclaimer @ origin/main |
|---|---|---|---|---|---|---|
| 1 | `/coach/operations` | `coach/FloorOperationsDesk.tsx` | **yes** (`:123`) | **yes** (`:125-126`) | **yes** | **yes** |
| 2 | `/admin/retro-lab` | `core/PunxsyEcosystemCore.tsx` + `core/DevToolsQAConsole.tsx` | **no** | partial — red pulsing ALERT (`PunxsyEcosystemCore.tsx:216-217`); `DevToolsQAConsole.tsx` has neither | **no** | **no change** |
| 3 | `/admin/macro-analytics` | `analytics/MacroCommandCenter.tsx` | **no** | **no** | **yes** (`:77`) | **yes** (`:78-83`) |
| 4 | `/board/dashboard` | `board/BoardViewportSwitcher.tsx` | **no** | **no** | **yes** (`:100`) | **yes** (`:101-105`) |
| 5 | `/admin/communications` | `communications/MediaAndCommsHub.tsx` | **no** | **no** | **yes** (`:39`) | **yes** (`:40-44`) |
| 6 | `/admin/curriculum` | `curriculum/CurriculumProgressionEngine.tsx` | **no** | **no** | **yes** (`:141`) | **yes** (`:142-145`) |

**The correct count of capability consoles carrying the disclosure stamp is
FIVE of six on current `origin/main`, and it was ONE of six at the commit the
pass read.** The one that still does not carry it is **`/admin/retro-lab`**.

**How I established it.** At HEAD, `grep -n "stamp"` over all seven components
returns a `stamp--brass` disclosure in exactly one —
`        <p className="mt-3"><span className="stamp stamp--brass stamp--flat">Planned — Not Yet Implemented</span></p>`
(`apps/web/src/components/coach/FloorOperationsDesk.tsx:123`), followed by
`          Every athlete, clearance, attendance figure, and matchup below is fabricated sample data.`
(`:125`). Both character-exact as the finding quotes them. The other `stamp`
hits in the sweep are false positives on identifiers (`timestampZulu`,
`FilmTimestamp`, `stamp: '00:42'`).

On `origin/main`, `git show origin/main:<file> | grep "stamp--brass\|fabricated
sample data"` returns the pair in five of the seven components. PR #422
("Declare the remaining prototypes, and stop losing sign-in silently") added an
identical four-line header block to `MacroCommandCenter`,
`BoardViewportSwitcher`, `MediaAndCommsHub` and `CurriculumProgressionEngine`,
plus four `*Honesty.test.tsx` files that lock it in
(`macroCommandCenterHonesty.test.tsx`, `boardViewportSwitcherHonesty.test.tsx`,
`mediaAndCommsHubHonesty.test.tsx`, `curriculumProgressionEngineHonesty.test.tsx`).

**The finding's named worst case is specifically fixed.** The finding argued that
`/admin/macro-analytics`'s invented SafeSport clearances were "the one that is
handled least". `origin/main` now renders, directly above that table:
`          and readiness figure below is fabricated sample data. Nothing on this page reads or writes`
(`MacroCommandCenter.tsx:80`) and, in the same paragraph, an explicit instruction
never to treat a name there as a cleared adult. That is a tighter disclosure than
the finding asked for.

**What the finding got right, verified line by line at HEAD.**
- `const IMMUNITY_GATE = 'Pending Coach Verification Flag {"verified_by_jason": false}';`
  (`MacroCommandCenter.tsx:25`) — character-exact, rendered in red at `:158` and
  `:162`, and the finding's reading of it is correct: it asserts a *pending
  verification*, not a fabrication.
- The two real-looking column headers at `:147-148` — character-exact.
- The file is 219 lines and, at HEAD, contains zero occurrences of
  planned/sample/fabricated/placeholder/mock/not yet implemented. Verified.
- "All six wrappers … are 14–21 lines and contain a role gate and a `<main>` and
  nothing else" — verified: 14, 18, 17, 21, 17, 17 lines respectively, each a
  `requirePageRole` plus a `<main>`.
- Four `layout.tsx` files, none carrying disclosure vocabulary — verified.

**Where the finding's table is harsher than the files.** Its binary "Disclaimer
present: no" for `/admin/communications` and `/admin/curriculum` flattens two
partial self-declarations that existed at HEAD:
`          <h2 className="text-xs uppercase tracking-[0.2em] text-slate-200">[L16] Public Portal Mockup [V-BOARD-PUBLIC]</h2>`
(`MediaAndCommsHub.tsx:43`) and `Issue Mock Badge`
(`CurriculumProgressionEngine.tsx:206`). Neither is a page-level fabrication
disclaimer and neither is a stamp, so "no" is defensible for the stamp column;
"no" for the disclaimer column is a judgement the table presents as a
measurement.

**Why OVERSTATED rather than RETRACTED (FIXED SINCE).** The brief's sentence
("*All six already carry a brass … stamp and a … disclaimer*") is **still not
true** on `origin/main` — `/admin/retro-lab` carries neither, only a red
pulsing alert reading
`          ⚠️ WARNING: Live access permissions detached. System running in mock-only front-end`
(`PunxsyEcosystemCore.tsx:216`). So a residual documentation inaccuracy remains
and the finding is not fully retracted. But the gap it reported is now one page
rather than five, the named safeguarding worst case is closed, and regression
tests hold the four new declarations in place. That is LOW, not HIGH.

**Fairness note that belongs on the record.** The finding was factually correct
when written, at the commit it names, and it self-checked for exactly this
("*Has it been fixed since the brief was written?*" — answered no, correctly, at
`04dd116b`). The downgrade is due to work merged after the pass ran, not to an
error by the pass. Its stated remedy — correct the sentence in
`docs/HANDOFF_VISUALS.md` — is still the right action, with "five of six, retro-lab
outstanding" as the corrected text.

---

### P7-04 [MEDIUM] `/admin/communications` — eleven inputs with no save path and no disclosure

**Verdict: OVERSTATED** — corrected severity **LOW**. The no-save-path half stands
exactly; the no-disclosure half was fixed by #422 on `origin/main`; and the control
count is wrong.

**Verified at HEAD, character-exact.** The file is 191 lines and contains **zero**
`<button`, **zero** `<form`, **zero** `onSubmit`, **zero** `fetch(` — I counted
each. The two quoted blocks are exact:

> `          <h2 className="text-xs uppercase tracking-[0.2em] text-slate-200">[L16] Public Portal Mockup [V-BOARD-PUBLIC]</h2>`
> — `apps/web/src/components/communications/MediaAndCommsHub.tsx:43`

> `              <p className="mt-1 text-[11px] text-zinc-500">Activates adaptive kinetic limiters</p>`
> — `apps/web/src/components/communications/MediaAndCommsHub.tsx:156`

The three pre-filled fields are real and at the cited lines — `useState` seeds at
`:26`, `:27` and `:28`, and the finding's reading is right: a staff member arrives
to what looks like saved content.

**Corrections.**

1. **Twelve controls, not eleven.** `grep -n "<input\|<textarea"` returns twelve
   hits: `:47`, `:54`, `:61`, `:75`, `:84`, `:93`, `:121`, `:131`, `:141`, `:157`,
   `:168`, `:179` — two `<input>` and ten `<textarea>`. The finding says "eleven"
   three times, including in its title.
2. **The gate is at line 11, not line 10.** `  await requirePageRole(['organization_admin', 'admin', 'staff']);`
   is `apps/web/app/admin/communications/page.tsx:11`; line 10 is the tail of the
   explanatory comment. The role list itself is exactly as the finding says, and
   it is genuinely the widest of the six consoles.
3. **The disclosure half is fixed since.** `origin/main` now renders, above every
   input: `        <p className="mt-3"><span className="stamp stamp--brass stamp--flat">Planned — Not Yet Implemented</span></p>`
   (`MediaAndCommsHub.tsx:39`) and `          Every contact, media item, and release status below is fabricated sample data. Nothing on`
   (`:40`), with a regression test at
   `apps/web/src/components/communications/mediaAndCommsHubHonesty.test.tsx`. A
   staff member on current `main` is told the page is a prototype before they type.

**Strongest refutation, and what remains.** *Is there a save path after all?* No —
`MediaAndCommsHub` is a default export taking no props, mounted bare at
`apps/web/app/admin/communications/page.tsx:15`. The typed-and-lost behaviour is
real on `main` today. But the harm the finding priced was "no error and no
warning"; the warning now exists, so what is left is a prototype that discards
input while announcing that it is a prototype. That is LOW.

---

### P7-05 [MEDIUM] The server's typed refusal vocabulary is discarded at the UI boundary

**Verdict: CONFIRMED WITH CORRECTION** — severity stays **MEDIUM**. One quotation
in this finding **does not exist in the file it is attributed to**.

**Verified independently, character-exact.**

> `  if (error instanceof MedicalStatusBlockedError) {`
> `    return NextResponse.json({ error: error.message }, { status: 409 });`
> — `apps/web/src/server/pilot/http.ts:98-99`

> `  if (error instanceof GuardianConsentMissingError) {`
> — `apps/web/src/server/pilot/http.ts:106`

The `PilotError` branch does emit the code:

> `      { error: error.message, ...(error.code ? { code: error.code } : {}) },`
> — `apps/web/src/server/pilot/http.ts:87`

The UI treatment is exact at both cited ranges:

> `        const err = (await response.json().catch(() => ({}))) as { error?: string };`
> `        throw new Error(err.error || `Entry failed (${response.status})`);`
> — `apps/web/app/operations/external-competition/page.tsx:228-229`

> `                <p className="alert-title">Failed</p>`
> — `apps/web/app/operations/external-competition/page.tsx:289`

and `grep -rn 'alert-title">Failed'` across `apps/web/app` and `apps/web/src`
returns **23** — the finding's number is exactly right.

**The correction — a fabricated quote.** The finding writes that `PilotError`
"*carries an ALL-CAPS machine `code` whose own comment says it exists "so a caller
can branch on the code rather than the prose"*". **That sentence is not in
`errors.ts`.** `grep -n "branch on the code\|rather than the prose"
apps/web/src/server/pilot/errors.ts` returns nothing. What the comment actually
says is:

> `     * The ALL_CAPS machine code, when the throw site had one. It belongs`
> `     * here rather than in the message prefix -- several existing messages`
> `     * read `SAFETY_FLAG_NOT_FOUND: prose`, which put the code exactly where`
> `     * the status matcher was looking and the prose exactly where nobody`
> `     * would see it.`
> — `apps/web/src/server/pilot/errors.ts:37-42`

The paraphrase is a *fair* reading of intent — the file's header does say the
point of the type is that a load-bearing contract should not be "expressed as a
string prefix and enforced by comment" (`errors.ts:11-12`) — but it is presented
inside quote marks as the comment's own words and it is not. Two smaller citation
slips in the same paragraph: the quoted `http.ts` block spans `98-108` (the
finding says `98-107`, dropping the closing brace it quotes), and the `PilotError`
branch is `85-90` (the finding says `84-89`).

**Second correction: the measured count.** "*1 file tests `response.status === 409`*"
— I find **two** non-test files: `apps/web/app/admin/people/page.tsx` and
`apps/web/src/lib/shadowFeedbackReview.ts`. "*0 files read `payload.code`*" —
confirmed, zero across `apps/web/app/**/*.tsx`.

**Strongest refutation, and it now points the other way.** The finding bounded
itself: "*PR #452 … adds the training-hold gate to `addCompetitionEntry`; on this
branch that gate does not exist, so today's refusals on these two screens are
tenancy and validation failures rather than safety ones.*" **#452 has since
merged** (`951030e1 Competition entry now asks whether the child may compete at
all`). I checked the UI side: `git diff HEAD origin/main --
apps/web/app/operations/external-competition/page.tsx` is **empty**. So the
condition the finding named as the trigger has occurred and the page was not
fixed — a real safety refusal from the competition-entry gate now renders in the
`✕ Failed` critical-red box. The refutation attempt strengthens the finding rather
than weakening it; MEDIUM is if anything generous, but raising it would depend on
Pass 5's route-level reading of #452 and I am not asserting that here.

---

### P7-06 [MEDIUM] Nine coach screens' only "back" control points at `/coach`, which is not a route

**Verdict: CONFIRMED** — severity stays **MEDIUM**.

Verified by exhaustive grep — exactly nine files, and **every one of the nine
cited line numbers is correct**:

> `            <Link href="/coach" className="btn btn--ghost">Back to Coach Workspace</Link>`
> — `apps/web/app/coach/transfer-check/page.tsx:177`, and identically at
> `intervention-executions/page.tsx:422`, `intervention-review/page.tsx:376`,
> `intelligence/page.tsx:169`, `one-percent-club/page.tsx:337`,
> `behavior-standards/page.tsx:268`, `intervention-protocols/page.tsx:307`,
> `attempt-log/page.tsx:278`, `floor-groups/page.tsx:336`

`apps/web/app/coach/` contains **24** subdirectories and **0** files at its own
level — verified with `find … -maxdepth 1 -type f | wc -l` → 0. So no `page.tsx`,
no `layout.tsx`, no `default.tsx`.

**Strongest refutation.** (a) *`buildingMap` has a `/coach` door.* Failed —
`grep -n "href: '/coach'"` returns nothing, and the file carries 103 `href:`
values, matching the finding's count exactly. (b) *A redirect exists.* Failed —
no `redirects` or `rewrites` in `apps/web/next.config*`, and **no `middleware.ts`
anywhere in `apps/web`**. (c) *`navigationFallback` rescues it.* `staticwebapp.config.json:44`
does declare one, and the finding's refusal to claim a production 404 on that
basis is the right call — I could not settle it either, and it belongs to Pass 11.

The finding's honesty about (c) is the reason this stays MEDIUM rather than
inflating: the claim it actually makes ("there is no route and no redirect in the
Next.js app") is fully proven.

---

### P7-07 [LOW] Six CSS classes in shipped markup exist in no stylesheet

**Verdict: CONFIRMED** — stays **LOW**.

> `      <h1 className="t-h1 mb-[var(--s4)]">The Bell</h1>`
> — `apps/web/app/auth/link/page.tsx:98`

Character-exact. `grep -c "t-h1"` returns **0** in both `design-system/ppbf.css`
and `apps/web/app/globals.css`. `.wall-names-year` with an exact boundary returns
**0** in both sheets while `.ledger-tape__edge` exists at
`apps/web/app/globals.css:666` — exactly the "nearest defined neighbour" pattern
the finding's table describes, including its explanation of why a naive `\b` grep
false-negatives.

The guard's narrowness is exactly as stated: `const OWNED_PREFIXES = [` at
`apps/web/components/designSystemClasses.test.ts:32`, listing the 14 prefixes at
`:33-46`, closing at `:47`. All 14 match the finding's list verbatim.

**Strongest refutation.** *One of the six is defined in some other `.css`.* Not
found for `t-h1` or `.wall-names-year`. The finding's table is the only place in
these three passes where a per-row "nearest neighbour" column made the claim
independently checkable, which is worth noting as good method.

---

### P7-08 [LOW] `app/admin/page.tsx` carries four raw hex values and four eyeballed type sizes

**Verdict: CONFIRMED** — stays **LOW**.

> `                  className="inline-flex h-11 items-center border border-[color:var(--brass-700)] bg-[var(--hide-900)] px-4 text-[14px] font-bold text-[color:var(--bone-100)] transition hover:bg-[#2a1a1a]"`
> — `apps/web/app/admin/page.tsx:1203`

Character-exact, and `grep -ic 2a1a1a` returns **0** in both stylesheets, so the
refutation the finding ran ("is it a token by another name?") fails the same way
for me.

---

### P7-09 [LOW] `/shadow` maps four evidence tiers to one identical off-token red

**Verdict: CONFIRMED** — stays **LOW**.

> `const STAMP_INK_BY_TIER: Record<ShadowEvidenceTier, string> = {`
> `  PROVEN: '#dc2626',`
> `  EMERGING: '#dc2626',`
> `  EXPERIMENTAL: '#dc2626',`
> `  RESEARCH_NEEDED: '#dc2626',`
> — `apps/web/app/shadow/page.tsx:99-103`

Character-exact, as is the five-line comment above it at `:94-98`. The reserved
ink is `  --stamp-red:  #A81E22;  /* refusal, redaction, destructive */`
(`design-system/ppbf.css:147`) — same line the finding cites, and the line also
carries `--locked`'s pairing at `:116`, so the finding's "also equals `--locked`"
is right. The self-refuting-comment reading is correct: the comment's only
justification for a per-tier map is that one tier needs a different ink, and all
four values are identical.

---

### P7-10 [LOW] The pipeline banner paints a completed stage in the safety ladder's locked ink

**Verdict: CONFIRMED** — stays **LOW**.

> `                    : isComplete`
> `                      ? 'border-[color:var(--brass-700)] bg-[var(--hide-800)] text-[var(--locked-ink)]'`
> — `apps/web/components/DevelopmentPipelineBanner.tsx:46-47`

Character-exact within the cited `44-50` range. The pair-member argument checks
out on one line:
`  --locked:     #A81E22;  --locked-ink:    #F3C9C6;` (`design-system/ppbf.css:116`)
— the two really are declared together, and `.badge--locked { --badge:
var(--locked); --badge-ink: var(--locked-ink); }` (`:670`) shows the system using
them as a pair. Using half of it as chrome is the misuse claimed.

---

### P7-11 [LOW] Three components with no importer, two of which the docs instruct future agents to use

**Verdict: CONFIRMED** — stays **LOW**.

Verified: `PaymentSetupBubble` (101 lines), `SkeletonLoader` (139), `TutorialButton`
(73) — all three line counts exact, and a grep for each identifier across
`apps/web/app`, `apps/web/components` and `apps/web/src`, excluding tests and each
component's own file, returns **zero importers** for all three.

> `- `SkeletonLoader` — loading placeholders (use these, don't invent spinners)`
> `- `TutorialButton` / `TutorialCard` — in-app help`
> — `docs/CANVAS_CONTEXT_PACK.md:75-76`

Character-exact. And the sibling really is still live:
`import TutorialCard from '@/components/TutorialCard';`
(`apps/web/app/help/page.tsx:2`).

---

### P7-12 [LOW] `/retro-lab` ships a theme toggle that does nothing and tells the user it does

**Verdict: CONFIRMED WITH CORRECTION** — stays **LOW**.

The page copy and the provider comment are both character-exact at their cited
ranges (`apps/web/app/retro-lab/page.tsx:71-77`;
`apps/web/components/ThemeProvider.tsx:13-19`), and the scoreboard really does
report a theme back to the user:
`      `OPEN: ${caseStamp === "HOLD" || !caseStamp ? 1 : 0} │ PRESENT: ${Object.values(attendance).filter((v) => v === "PRESENT").length} │ THEME: ${isRetro ? "RETRO" : "TACTICAL"}`,`
(`apps/web/app/retro-lab/page.tsx:59`) and `              {isRetro ? "RETRO ON" : "TACTICAL"}` (`:89`).

**Correction.** The finding states: "*Verified: `grep -rn 'data-theme'` across
`design-system/ppbf.css` and `apps/web/app/globals.css` returns zero hits.*" It
returns **one** hit — `apps/web/app/globals.css:23`:
`     no [data-theme] override: one look, the golden-era one. Law 6's ink ground`.
It is inside a comment, so no *rule* responds to `data-theme` and the substantive
claim survives intact. But a mechanical verification quoted as "zero hits" that is
not zero is the kind of claim a reader will re-run, and this is the second such
count in this pass (see P7-04's "eleven").

---

### P7-13 [LOW] `TrainingHoldBanner` fails silent in both directions

**Verdict: CONFIRMED** — stays **LOW**. This is the best-argued finding in the
pass: it runs the refutation, reports that the refutation *largely succeeds*, and
records at LOW anyway with the reason stated.

Verified character-exact:

> `        if (!response.ok) return;`
> `        const payload = (await response.json()) as { hold?: AthleteFacingHold | null };`
> `        if (payload.hold) setHold(payload.hold);`
> `      } catch {`
> `        // Render nothing on failure: an unreachable API is not a hold.`
> — `apps/web/components/TrainingHoldBanner.tsx:48-52`

and `  if (!hold) return null;` at `:58`.

**Quote flag.** The file header is quoted with an em dash — *"(or when the fetch
fails — an error here…)"* — where the file has a double hyphen:
` * when there is no active hold (or when the fetch fails -- an error here`
(`apps/web/components/TrainingHoldBanner.tsx:11`). Punctuation normalised inside
quote marks. The same normalisation appears in several quotes across all three
passes; noted once here rather than repeated.

The three-states argument is correct on the code as written: `!response.ok` and
the `catch` both fall through to `hold === null`, which renders identically to
"not held".

---

## Pass 9

### F-9-01 [HIGH] `/operations` presents LEGACY-READINESS as a signed, certified, active mathematical gate, to every role in the gym

**Verdict: CONFIRMED** — severity stays **HIGH**. I attacked both halves the task
named (role gating, and whether the "certified/signed" presentation is what the
pass says) and neither broke.

**The registry prohibition, character-exact at the cited range.**

> `    expression: 'clamp(1, 10, sleep × 1.25 − soreness × 0.45 + discipline × 0.3)',`
> `    support: 'experimental_unsupported',`
> `    outputUnit: 'unitless',`
> `    humanReviewRequired: true,`
> `    unsupportedReason: 'Coefficients, input scales, fairness, and clinical/safety validity are unproven. It must not clear, restrict, or prescribe training.',`
> — `apps/web/src/server/pilot/formulas/registry.ts:315-319`

**The screen, character-exact at every cited line.**

> `const shadowReadinessEquation = 'Readiness = max(1, min(10, (Sleep x 1.25) - (Soreness x 0.45) + (Discipline x 0.3)))';`
> — `apps/web/app/operations/page.tsx:112`

> `                SHADOW v21.1 seed is ingested, stress-validated, and sealed for development deployment. This build section mirrors the certified guardrails used for floor safety, role isolation, and audit integrity.`
> — `apps/web/app/operations/page.tsx:242`

It is rendered inside an article whose heading is at `:255` —
`                <h3 className="t-command" style={{ fontSize: 'var(--t-md)' }}>Mathematical Gate Validation</h3>`
— with the equation immediately beneath at `:256`. The stamp copy is exact at
`:304`.

**Role gating — I checked it independently and the finding is right.** The gate is
`    <RoleSessionGate allowedRoles={operationsRoles}>` (`:207`) over
`const operationsRoles: ClubRole[] = [...roleRoutes.map((route) => route.role), 'platform_owner'];`
(`:203`), and `apps/web/components/roleRoutes.ts` declares `role: 'athlete',`
(`:36`), `role: 'coach',` (`:42`), `role: 'parent',` (`:48`), `role: 'admin',`
(`:54`), `role: 'staff',` (`:60`), `role: 'volunteer',` (`:66`), `role: 'board',`
(`:72`) and eight named board seats (`:78-120`). **An athlete and a guardian are
both in the allow-list.** Confirmed.

**Strongest refutation.**

1. *Is "certified" scoped to something narrower than the equation?* Failed. The
   stamp copy names "equations" explicitly, and the introducing paragraph at `:242`
   says the section "mirrors the certified guardrails". Both are page-level copy
   above the equation.
2. *Is the `<details>` collapse enough?* No — one click, from any role. The finding
   already concedes this is a mitigation not a defence, correctly.
3. *Is the false boundary check actually false?* Both are, and I verified both
   independently.
   - `isDeltaRPELocked` is character-exact at its cited range:
     `export function isDeltaRPELocked(deltaRpe: number, rationale: string | null): boolean {`
     (`apps/web/src/server/pilot/readinessMath.ts:25`) through `:31`, and it has no
     caller. The "uncallable" claim also holds: my own
     `grep -rni "rpe_intended|intendedRpe|target_rpe|planned_rpe|prescribed_rpe"`
     over `infra/azure/*.sql` and `apps/web/src/server/pilot/*.ts` returns
     **nothing**, so `calculateDeltaRPE(rpeObserved, rpeIntended)`'s second operand
     has no persisted source anywhere. This is the sharpest single check in Pass 9.
   - The 5.0 claim: the only readiness band in the platform that changes a session
     is `>= 7` / `>= 5` in a React component (see F-9-02). No 5.0 trigger exists.
4. *Fixed since?* No. `git diff HEAD origin/main -- apps/web/app/operations
   apps/web/src/server/pilot/formulas` is empty.

**Small precision note.** The finding says `roleRoutes.ts` lists "athlete, coach,
parent, admin, staff, volunteer and eight board seats" — fourteen. There are
**fifteen**; it omits the generic `board` role. Same undercount as Pass 7's
version of this sentence. It does not affect the claim, which is "every role".

---

### F-9-02 [HIGH] The one readiness number that actually changes a child's training is a client-side constant, defaulting to GREEN

**Verdict: CONFIRMED** — severity stays **HIGH**. The trace holds end to end and
one thing is **worse** than the finding states.

**It is truly client-side.** The band function is character-exact:

> `function getReadinessLevel(readinessToTrain: number): ReadinessLevel {`
> `  if (readinessToTrain >= 7) return 'GREEN';`
> `  if (readinessToTrain >= 5) return 'YELLOW';`
> `  return 'RED';`
> `}`
> — `apps/web/components/AthleteWorkspace.tsx:269-273`

and the default is character-exact:

> `  const [readinessToTrain, setReadinessToTrain] = useState(8);`
> — `apps/web/components/AthleteWorkspace.tsx:577`

`getReadinessLevel(8)` → `'GREEN'`. Confirmed.

**What GREEN changes, verified line by line.**

> `      description: readiness === 'GREEN'`
> `        ? 'Footwork progression + combination reps at normal intensity.'`
> `        : 'Controlled technical reps with clean form and reduced impact output.',`
> — `apps/web/components/AthleteWorkspace.tsx:316-318`

That is the child's prescribed technical block. The conditioning branch swaps
"Conditioning Finisher" / high-output intervals for "Recovery Conditioning" /
low-impact work at `:331-344`, as cited.

**Does any server path override or validate it? No — and this is where the finding
understates.** I read the whole `POST` handler
(`apps/web/app/api/pilot/floor-plans/route.ts:90-158`). It validates four things:
role (`:93`), athlete presence (`:105`), plan presence and type (`:109`), payload
size against `MAX_PLAN_BYTES` (`:114-117`), plus `assertActorCanAccessAthlete`
(`:119`) and a parseability guard on `generatedAt` (`:124-127`). It never
recomputes the band and never validates the readiness value. It then writes the
client's string into a **dedicated first-class column**, not merely into the
`jsonb` blob:

> `        plan.readiness || 'UNKNOWN',`
> — `apps/web/app/api/pilot/floor-plans/route.ts:149`

and that column has **no CHECK constraint**:

> `  readiness              text not null,`
> — `infra/azure/pilot_slice_postgres.sql:865`

So a client-asserted readiness band, computed by a threshold that exists only in
JSX, is stored in a queryable column with no vocabulary constraint, and served
back to the athlete, their coach and org admins by the `GET` at
`floor-plans/route.ts:30-84`. The finding says "stores the whole object as
`jsonb`"; the dedicated `readiness` column makes it a promoted, filterable field.
That strengthens the finding.

**Strongest refutation, and why each failed.**

1. *A server module re-derives the band from `pilot.readiness`.* Failed — no
   import of `readinessBoard.ts` in `AthleteWorkspace.tsx`, and the floor-plan
   route imports no formula module at all.
2. *`7` and `5` are pinned by a test or sourced from the registry.* Failed. They
   appear once each in the component, uncommented.
3. *The codebase does not otherwise know that reassuring defaults are unsafe.*
   Failed decisively, and the finding's two citations are exact:
   `// comment ("never default these to a reassuring value"):`
   (`apps/web/src/server/pilot/readinessBoard.ts:9`) and
   `                      <p className="t-muted">No fresh readiness check-ins -- do not read this as &quot;zero flags&quot;</p>`
   (`apps/web/components/CoachWorkspace.tsx:1635`). The platform states the rule
   twice and breaks it on the child's own screen.
4. *Fixed since?* No. `git diff HEAD origin/main -- apps/web/components/AthleteWorkspace.tsx
   apps/web/app/api/pilot/floor-plans` is empty.

**On the HIGH-vs-CRITICAL question.** The finding's own reasoning for stopping at
HIGH — no safety gate cleared, no hold lifted, no contact authorised, a task list
with a coach on the floor — is the right test and I reach the same answer. I note
one fact the finding does not use, which a reviewer arguing for CRITICAL would
want: the band is not only rendered, it is **persisted to a named column and
readable by the coach**, so the reassuring default outlives the screen. That
argues the finding was, if anything, conservative.

---

### F-9-03 [HIGH] "Readiness to Train" is stored in a column named `rpe` and displayed to the child as "effort"

**Verdict: CONFIRMED WITH CORRECTION** — severity stays **HIGH**. Both labels and
the column verify exactly; one line citation is off by two.

**The column.** Character-exact:

> `  rpe numeric not null,`
> — `infra/azure/pilot_slice_postgres.sql:96`

No CHECK, no scale declaration — verified by reading the surrounding `create
table` block.

**Label one — what the child is asked.** Character-exact:

> `                      <label className="t-label block mb-[var(--s3)]" htmlFor="readiness-train">Readiness to Train (1-10)</label>`
> — `apps/web/components/AthleteWorkspace.tsx:1772`

**Label two — what the child is shown back.** Character-exact:

> `          const ink = Math.min(Math.max((s.rpe || 0) / 10, 0), 1);`
> `          const label = done`
> `            ? `Session ${formatDate(s.date)}, effort ${s.rpe} of 10``
> — `apps/web/components/TrainingCard.tsx:186-188`

Both labels confirmed. The ink-density inversion the finding describes is real:
the `ink` on line 186 scales directly with the value, so a higher *readiness*
prints a darker stamp.

**The write path.** `          rpe: readinessToTrain,` occurs at exactly the three
cited sites — `AthleteWorkspace.tsx:1266`, `:1282` and `:1311` — and the formula
observation is character-exact:

> `      kind: 'session_rpe' as const,`
> `      value: input.rpe,`
> `      unit: 'rpe_0_10' as const,`
> — `apps/web/components/AthleteWorkspace.tsx:383-385`

The in-row corroboration is character-exact too:

> `function autoCheckInNote(readiness: ReadinessLevel): string {`
> `  return `Auto check-in readiness ${readiness}`;`
> `}`
> — `apps/web/components/AthleteWorkspace.tsx:227-229`

**The downstream consequences.** `                    <th className="t-label p-[var(--s3)]">Avg RPE</th>`
(`apps/web/app/coach/performance-analytics/page.tsx:137`) — exact. The load
formula's expression is exact: `    expression: 'session RPE × duration minutes',`
(`apps/web/src/server/pilot/formulas/registry.ts:62`), consumed by
`export function calculateSessionLoad(input: {`
(`apps/web/src/server/pilot/formulas/engine.ts:255`).

**Corrections.**

- **`avg(rpe)` is at line 112, not 110.**
  `              avg(rpe)::float8 as avg_rpe`
  is `apps/web/src/server/pilot/performanceAnalytics.ts:112`. Lines 110-111 are
  the `count(*)` aggregates. Off by two.
- **The panel it names does not exist where it says.** F-9-03's refutation (1)
  argues the slider "*sits in the "Daily Biological Check-In" group beside Sleep,
  Energy, Motivation and Soreness (`:1760-1775`)*". The panel containing the
  Readiness-to-Train slider is headed
  `                  <h3 className="t-label mb-[var(--s4)]">Current Readiness</h3>`
  (`apps/web/components/AthleteWorkspace.tsx:1759`). "Daily Biological Check-In"
  is a **different** panel further down —
  `                <h3 className="t-label">Daily Biological Check-In</h3>`
  (`:2309`) — and Motivation (`:2324`) and the Daily-Check-In Soreness slider
  (`:2329`) live there, not in `1760-1775`. Within the cited range there are three
  controls: Sleep (hours) (`:1762`), Energy Level (1-10) (`:1767`) and Readiness to
  Train (1-10) (`:1772`). The argument survives — the slider *is* grouped with
  wellness inputs and collected pre-session — but the panel name is wrong and two
  of the four named siblings are in a different panel. Note also that Pass 9's own
  F-9-05 describes the "Daily Biological Check-In" panel correctly as Sleep,
  Hydration, Motivation, Soreness, so the two findings disagree with each other
  about which panel holds what.
- The finding's `validation.ts:61-66` citation for `requireNumber` is exact
  (`function requireNumber(value: unknown, field: string): number {` at `:61`
  through `:66`), and its reading is right — only "is a number" is enforced.

**Strongest refutation, and it fails in the finding's favour.** *Is there a real
RPE writer that makes the column mostly correct?* There is one, and the finding
already names it honestly: `POST /api/pilot/sessions` is open to
`['organization_admin', 'coach', 'athlete']`
(`apps/web/app/api/pilot/sessions/route.ts:14`) and routes through
`validateSessionPayload` into `upsertSession`, whose insert names the column
(`apps/web/src/server/pilot/entities.ts:176`). So the column genuinely holds two
different constructs. The finding's conclusion — that this makes `avg(rpe)` an
average over two incompatible quantities and is therefore *worse*, not better —
is the correct reading and I could not improve on it.

