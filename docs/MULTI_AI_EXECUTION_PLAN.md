# Multi-AI Execution Plan — SUPERSEDED

This document is retained only as historical context for the earlier SHADOW function-first build process.

It is **not an active agent instruction source**.

Current execution starts at [`../AGENT_KERNEL.md`](../AGENT_KERNEL.md). Current work ownership/state lives in [`current/WORK_QUEUE.md`](current/WORK_QUEUE.md). Shipping and gatekeeper operations live in [`AI_DELIVERY_PIPELINE.md`](AI_DELIVERY_PIPELINE.md).

Do not reconstruct the old Architect -> Implementer -> Reviewer -> QA workflow, locked build order, cadence rules, or command list from this file unless a historical audit specifically asks for them.

Reason for supersession: the active delivery pipeline and current work queue absorbed the useful controls from this plan, while keeping both active forced agents to reconcile overlapping role, ticket, gate, and sequencing rules.
