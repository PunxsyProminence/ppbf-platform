name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      CI: true

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Run all fast regression tests
        run: npm test

      # Each .pg suite runs in its own Jest process (see the test:migrations:*
      # scripts). They spin up separate embedded Postgres instances and load
      # ESM-only modules through a native dynamic import; sharing one process
      # made a suite's import resolve after another suite's environment had been
      # torn down. --runInBand is therefore implicit -- each invocation runs a
      # single file -- so no extra flags are passed through here.
      - name: Run PostgreSQL migration suite
        run: npm run test:migrations

      - name: Install Playwright browsers
        run: npx --workspace web playwright install --with-deps chromium

      - name: Run board governance E2E
        run: npm --workspace web run test:e2e:board

      # The public entry points, plus the only screenshot baseline in the repo.
      # It existed and ran nowhere, so its baselines were recorded on a Windows
      # machine and had drifted far enough that the spec could not pass at all.
      # Regenerated on Linux and wired in here so a visual change to the
      # homepage or The Bell has to show up as a diff rather than be noticed by
      # somebody happening to look. Both baselines are committed; the config
      # allows a 2% pixel ratio so a Chromium revision bump does not read as a
      # regression, which is well under what any real layout change moves.
      - name: Run public homepage E2E
        run: npm --workspace web run test:e2e:homepage

      - name: Build production application
        run: npm run build
