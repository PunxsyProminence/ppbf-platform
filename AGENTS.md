# Agent entrypoint

Read `AGENT_KERNEL.md` first. It is the single default execution contract for this repository.

Do not preload unrelated policy or historical documents. Load specialized rules only when the current ticket or changed surface requires them.
